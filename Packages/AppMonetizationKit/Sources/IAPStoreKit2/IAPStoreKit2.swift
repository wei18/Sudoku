// IAPStoreKit2 module anchor.
//
// Real content (LiveStoreKit2IAPClient, IAPProductMapper) lands in Phase v2.1
// per docs/v2/plan.md. This empty namespace exists so `swift build` resolves
// a non-empty module from v2.0 onward.
//
// Intentionally left empty until v2.1.1 (LiveStoreKit2IAPClient skeleton).
// See `docs/v2/plan.md` §Phase v2.1.

internal enum ModuleAnchor {}
