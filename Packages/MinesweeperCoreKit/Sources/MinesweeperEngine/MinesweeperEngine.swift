internal import Foundation

// MARK: - MinesweeperEngine (skeleton)
//
// PR D placeholder — exists so `Project.swift`'s Minesweeper target links a
// real product and `tuist generate` produces both schemes. Real mine
// placement, reveal flood-fill, and win/lose detection land in follow-up PRs.

public struct MinesweeperEngine: Sendable {
    public init() {}
}
