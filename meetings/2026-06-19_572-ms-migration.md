# #572 — Minesweeper migration to GameConfig/makeGameApp (SDD-005 Pillar C, MS)

**Date:** 2026-06-19 · **Status:** implementing · Closes #554 · Parent #479
**Template:** the Sudoku migration — `SudokuKit/Sources/SudokuAppComposition/Live.swift` + `AppComposition.swift` (#556 backbone + #557 shared Home). MS mirrors it exactly; this is the SECOND app through the proven path.

## Audit
`MinesweeperRoot.swift` is a near-exact mirror of Sudoku's pre-#557 `RootView`: `GameRoot` + GC-signed-out `.alert` + `MinesweeperHomeView` rootContent. The ONLY divergences from Sudoku are the two **capability gaps this migration fills**: MS has **no `.attPrimerSheet`** (missing ATT, 5.1.2 blocker) and **no ResumePill** in Home (#554). All MS *gameplay* divergence (isFailed/replayDailyBoard, win/loss board-overlay completion, no-mistakes, sync daily provider) lives in `LiveRouteFactory` + board + completion → **stays per-game**, untouched.

## Task (mirror Sudoku's migrated composition)
1. Build a Minesweeper `GameConfig<AppRoute>` in `MinesweeperAppComposition`:
   - subsystem `com.wei18.minesweeper`; ckConfig `.minesweeper`; removeAdsProductId (`minesweeperRemoveAdsProductId`); theme `MinesweeperTheme()`; title "Minesweeper"; sidebar/home mode cards (from `MinesweeperHomeViewModel.modeItems` subtitles + routes); successTint/failureTint (MS theme); audio `AudioConfig(keyPrefix: "com.wei18.minesweeper.audio")`; reminder `ReminderContentConfig` (MS copy from the current reminder wiring); `fetchResume` (MS `MinesweeperSavedGameStore` → `ResumeCandidate` — port `Live+Resume.swift`'s `makeFetchResume`); `makeRouteFactory` = MS `LiveRouteFactory` (unchanged); `makeHome` = shared `GameHomeView`; `presentLeaderboard: { GameCenterDashboard.present() }` (shared, #560); `reminderTapRoute` if MS deep-links.
2. `MinesweeperAppComposition.live()` → build the config + `makeGameAppWithDeps`, mount `wired.view` (mirror Sudoku's `AppComposition.rootView`). Keep the public `MinesweeperAppComposition` field shape its tests/App read.
3. **Delete** the asymmetric per-app files (their content folds into config/makeGameApp): `Live+Resume.swift`, `Live+Audio.swift`, `MinesweeperRoot.swift`, `MinesweeperRootViewModel.swift`, `MinesweeperHomeView.swift`, `MinesweeperHomeViewModel.swift`. **Keep** `LiveRouteFactory.swift` + `LiveRouteFactory+Helpers.swift` (per-game routes; already on `boardDestination` from #559).
4. **Adopt the shared `ReminderSettingsEntry`** (SettingsUI, from #556) — delete `MinesweeperReminderSettingsEntry`; MS `SettingsView` uses the shared type. (This folds the #560 Settings-wrapper collapse.)
5. **Capability gaps filled by makeGameApp (universal):** MS now gets the shared **ResumePill mount** (closes #554) + the **ATT primer** + the GC-signed-out alert (all wired in makeGameApp). Confirm MS's banner ATT-priming + the ATT sheet render with MS theme.

## ⚠️ Behaviour/pixel-preserving + the #554 caveat
- Snapshot-gated: MS Home/Root snapshot tests that construct `MinesweeperHomeView`/`MinesweeperRoot` migrate to the shared `GameHomeView`/`wired.view`; baselines must NOT move except the wrapper type-name `.txt` deltas (same as Sudoku #557). Any PNG move = STOP.
- MS gameplay surfaces (board, completion overlay, LiveRouteFactory routes) unchanged → their snapshots must be byte-identical.
- #554's persistence half was confirmed CORRECT (see #554 comment) — the render-site (ResumePill mount) is the fix. Leader will sim-verify (iCloud-signed-in): play MS to a save → Home ResumePill surfaces; ATT prompt fires at first ad context.

## Verification
- `swift build` GameAppKit (unchanged) + MinesweeperKit + GameCenterKit green.
- `swift test` MinesweeperKit — all pass; **0 snapshot PNG changes** (only wrapper type-name `.txt` deltas allowed). `--no-parallel` for the known SudokuKit-style flake if it appears.
- swiftlint --strict clean.
- Leader post-merge sim-verify on iCloud-signed-in sim: #554 ResumePill + ATT prompt + audio/reminder settings + Daily(isFailed/replay)/Practice/Completion(win/loss) intact.

## Out of scope
2048 migration (its own PR — also adds 2048's audio+reminders capability gaps).
