// AdsAdMob module anchor.
//
// Real content (LiveAdMobAdProvider, ATTPresenter) lands in Phase v2.2 per
// docs/v2/plan.md. This empty namespace exists so `swift build` resolves a
// non-empty module from v2.0 onward, keeping the dep graph in Package.swift
// addressable.
//
// Intentionally left empty until v2.2.1 (LiveAdMobAdProvider skeleton). See
// `docs/v2/plan.md` §Phase v2.2.

internal enum ModuleAnchor {}
