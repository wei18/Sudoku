# Impl Notes — #1020 Stage A (shared shell + VM contracts) (2026-08-24)

Status: COMPLETE
Owner: Developer subagent (Stage A)
Dispatched by: Leader
Started: 2026-08-24T23:55+08:00
Finished: 2026-08-25

Scope: `Packages/GameShellKit` + `Packages/GameAppKit` only. SudokuKit /
MinesweeperKit are expected NOT to compile at the end of this stage (Stage B
migrates them onto the API landed here).

## 設計決定 (Design decisions)

- **`AppTab` SF Symbols = `calendar` / `dice` / `chart.bar`** — dispatch says
  "reuse the repo's existing daily/practice/stats sidebar icons". `HomeMode`
  (`GameShellUI/Home/HomeScreen.swift`, being deleted) used `calendar` for
  `.daily` and `dice` for `.practice`; the `#773`/`#844` Statistics entry used
  `chart.bar` (`GameAppKit/GameHomeView.statsLink`). `docs/designs/v3/prototype.html`
  draws Today as a calendar, Progress as a bar chart, and Practice as a 9-cell
  grid — the first two agree with the repo symbols; for Practice the repo's
  `dice` is kept per the dispatch's "reuse" instruction rather than switching to
  `square.grid.3x3`. Flagging for Leader: prototype vs repo disagree on Practice
  only.

- **Tab-title / "Achievements" strings live in the two APP catalogs, not a
  GameShellUI catalog** — GameShellUI ships **no** string catalog at all (see the
  `HomeMode.titleKey` doc: "GameShellUI ships no string catalog of its own";
  `Text(LocalizedStringKey)` resolves against `Bundle.main`). So the only place
  these keys can live is `App/Sudoku/Resources/Localizable.xcstrings` and
  `App/Minesweeper/Resources/Localizable.xcstrings`. See §偏離 for the fixture
  consequence.

- **`HomeModeCard` / `HomeScreen` deleted wholesale** — verified by grep that the
  only *code* consumer of anything in `Home/HomeScreen.swift` outside the deleted
  `GameHomeView` is nothing; every other hit across SudokuKit / MinesweeperKit /
  `GameShellUI/DailyHubShellView.swift` is a **comment** referencing
  `HomeModeCard.cardPadding` / `HomeScreen.cardGridGap` as a spacing precedent.
  Those comments become stale references outside my scope (Stage B / a later
  doc pass); nothing stops compiling.

- **`LastSelectionStore` KEPT** — the dispatch said "delete if it only served
  home-mode memory". It does not: it backs Practice-difficulty persistence in
  `SudokuAppComposition/LiveRouteFactory.swift`,
  `MinesweeperAppComposition/LiveRouteFactory.swift`, and
  `MinesweeperUI/MinesweeperBoardView.swift` (tap-mode). Deleting it would break
  both apps for an unrelated reason. Verified by grep before deciding.

- **`GameConfig.title` removed** — after the rewrite nothing in
  GameShellKit/GameAppKit consumes it (`RootShellView` no longer takes a title;
  `GameHomeView.navigationTitle(title)` is deleted). Per the dispatch's "keep
  only if something still consumes it" and the repo's no-compatibility-shim
  principle, it is gone. Stage B sets per-tab titles with `.navigationTitle` in
  its own tab-root views.

- **Overlay hoisting registers only on the `false→true` edge** — so the hoisted
  view keeps a stable SwiftUI identity (its own `@State`, e.g. the completion
  surface's local animation flags) for as long as it is up. Consequence,
  documented in the API doc comment: values read *eagerly* while building
  `content()` are snapshotted at registration time. This is safe because
  `isActive` is by construction the disjunction of exactly the branches inside
  `content()`, so the branch taken at registration is the branch that made it
  active; `@Observable` reads inside the hoisted body stay live (observation
  registers on the hoisted view's own body evaluation) and `@Binding` / captured
  closures write through to live storage.

- **The iOS `fullScreenCover` board deliberately sees `coordinator == nil`** —
  `RootShellView` injects `\.boardModalOverlayCoordinator` *inside* its own body
  (onto the `TabView`), while `GameRoot` attaches `.fullScreenCover` to the
  **result** of `RootShellView`. Environment flows from the attachment point, so
  cover content inherits the default `nil` and the modifier renders in place —
  exactly the pre-#1020 behavior iOS already had, and the reason #1019's naive
  port only broke the pushed (macOS/iPad) case.

## 設計決定, 續 (Leader addenda, received mid-task)

- **`UITestLaunchTarget` replaces the `(String) -> Route?` resolve closure** —
  `-uitest-route daily|practice|settings|resumeFail` is how both E2E suites
  launch, and `daily` / `practice` named routes that are tab identities now, so
  a `Route?`-returning closure can no longer express them. The seam resolves
  into `enum UITestLaunchTarget { case tab(AppTab); case push(Route, tab: AppTab) }`
  with a `.push(route)` shorthand defaulting to `.today`. The launch-ARGUMENT key
  string is unchanged, so existing invocations keep working; only the per-app
  `resolve` closure's return type changes (Stage B).
  - Label is `tab:`, not the Leader's suggested `in:` — SwiftLint `identifier_name`
    hard-fails 1–2 character identifiers and rejected `in`.
  - Swift enum cases cannot carry default associated values, hence the static
    `push(_:)` factory rather than `case push(Route, tab: AppTab = .today)`.
  - The effect is factored into `UITestRouteModifier.apply(_:)` so it is testable
    without rendering, and the "replace, never append" property (a re-fired
    `onAppear` must not stack duplicates) is now pinned by a test rather than a
    comment.

- **`game.today.root` on `TodayTabHost`** — replaces the retired HOME scaffold's
  `game.home.root` (#935) as the locale-independent landing anchor for E2E
  "left the board, came back" assertions. `TodayTabHost` takes only plain
  injected inputs (root VM, adProvider, adGate, attPrimer, content), so it
  renders standalone in a snapshot host without the `makeGameApp` stack.

## 設計決定, 續 2 (Leader review fix — tab-root memoization)

- **`makeTabRoot` is now invoked exactly once per tab, at composition time.**
  Leader review caught a MAJOR defect in my first cut: `tabRoot` was a plain
  closure that re-ran `config.makeTabRoot(tab, deps, rootViewModel)` on every
  call, and `RootShellView.tabStack(for:)` calls it on every body evaluation.
  Stage B builds its hub view models (DailyHub / PracticeHub / Stats) inside
  that closure, so every render would have minted fresh models and wiped hub
  state — the #918 shape (`ReminderSettingsEntry` as a per-render factory,
  where an ancestor re-render replaced the model the user had just tapped and
  the primer sheet dismissed itself).
  - Fix: `memoizedTabRoots(_:)` in `MakeGameApp+Helpers.swift` builds all three
    roots into a `Dictionary` keyed by `AppTab` and returns a lookup closure.
    The Today wrapping happens inside the builder, so it is applied once too.
  - No force-unwrap: `AppTab.allCases` makes the dictionary total, and the
    `?? AnyView(EmptyView())` fallback exists so a future case added without
    rebuilding renders empty instead of trapping in a shipped app.
  - **Contract now documented on the helper**: `GameConfig.makeTabRoot` is
    invoked once per tab per launch and MUST be treated as a factory whose
    products have stable object identity. Stage B depends on this.
  - Accepted consequence: all three tab roots (and their view models) are
    constructed at launch, including tabs the player never opens. Lazy +
    memoized would only move the same allocation to first visit while adding a
    mutable cache, so eager is the simpler shape with identical steady state.
  - Test seam: `makeGameAppCore` cannot be called from a unit test — it wires
    the live CloudKit/GameKit/AdMob stack, and constructing that in an
    unentitled runner is the uncatchable-`CKException` trap CLAUDE.md warns
    about. `memoizedTabRoots` is the minimal seam the composition root routes
    through; `MemoizedTabRootsTests` asserts 3 builder calls total across 5
    further evaluations of every tab, plus per-tab instance stability.

## 設計決定, 續 3 (Follow-up: shared Settings gear)

Interactive iPad check found NO Settings entry point anywhere in the new shell —
the retired HOME's Settings card was the only one. design.md §2.1 / §3.7 require
a trailing toolbar gear on every tab ("每 tab 右上齒輪").

- **`GameConfig.settingsRoute: Route`, required, no default.** A game that
  silently shipped without it would have Settings *unreachable* on iPhone, not
  merely degraded — so this is a compile error for every game, by design.
- **`TabRootChrome` (new GameAppKit file), not inline in `MakeGameApp`.** That
  file sits at 386/400 lines, and the chrome needs its own testable seam anyway.
  Applied from ONE place (`chromedTabRoots`) rather than by each game's tab
  views, so the two apps cannot drift on which tabs have the gear — the exact
  per-app-wrapper drift the mirror principle exists to prevent.
- **"Every tab is chromed" is enforced by the TYPE CHECKER, not a test.**
  `chromedTabRoots` returns `(AppTab) -> ModifiedContent<AnyView, TabRootChrome<Route>>`
  instead of erasing to `AnyView`. One closure serves all three tabs, so there is
  no per-tab path that could skip the chrome, and the test's type annotation is
  itself the assertion. This avoided a brittle `Mirror`-spelunking test through
  `AnyView`, which cannot see the wrapped type.
- **`memoizedTabRoots` became generic** (`<TabRoot: View>`) to carry the
  unerased type through. Its fallback changed from `AnyView(EmptyView())` to
  `build(tab)` — still non-force, still unreachable (`allCases` makes the
  dictionary total), and it now degrades to the old per-render behavior rather
  than to a blank screen if a future case is added without rebuilding. Cost:
  `build` had to become `@escaping`.
- **The gear pushes via `rootViewModel.push`**, which targets `selectedTab` —
  by construction the tab whose gear was tapped — so Settings pushes WITHIN the
  initiating tab and Back returns there (§2.1's per-tab paths). Not a tab switch,
  not a shared modal.
- **`.toolbar` must sit inside the tab's `NavigationStack`.** `RootShellView`
  mounts `tabRoot(_:)` as the stack's root content, so applying the chrome to
  the tab root is correct; attached outside the stack it would have no navigation
  bar to render into and would silently disappear. Documented on the modifier.

## 除錯中 (macOS pause blocker — IN PROGRESS, root cause NOT yet confirmed)

Symptom (macOS native XCUITest): pause on a PUSHED board → no overlay, board
gone, Practice hub restored.

**Instrumented ground truth** (NSLog at every boundary; real app, pid 73198,
`log show --predicate 'process == "Sudoku"'`):

```
:47.681  modifier.onChange isActive=1 coord=1
:47.681  coordinator.present                    ← overlay IS registered
:47.683  tabStack practice count=1              ← path intact
:47.689  shell.onPreferenceChange 1
:47.713  tabStack practice count=1  (flag true)
:47.719  shell.onPreferenceChange 0             ← board stops contributing
:47.742  modifier.onDisappear isActive=1        ← board unmounted, still "active"
:47.746  coordinator.dismiss had=1              ← overlay destroyed by my hook
:47.754  tabStack practice count=1              ← path STILL [.board]
```

**Finding that reframes the bug: `setPath` NEVER fires a shrink.** The tab's
path binding holds `[.board]` throughout — before, during and after. So "the
whole stack pops" is the wrong model: the navigation MODEL is intact and the
DESTINATION VIEW is being unmounted underneath it. The hub is what a
NavigationStack renders when its path entry has no live destination.

**Hypotheses tested and REJECTED (single-variable, real app):**
- *(Leader's #1)* `setPath` receives a shorter path → **rejected**: no shrink
  log at all, `handlePathShrink` never runs.
- *(Leader's #2)* `.disabled(isBoardModalOverlayActive)` tears down the stack →
  **rejected**: replaced with `.disabled(false)`, failure reproduced
  byte-identically (run `mac-diag-4`).
- *(local harness)* ancestor observing `coordinator.content` → rejected there,
  but the harness itself proved INVALID (see below), so this needs re-testing
  in the real app.

**Leading hypothesis (untested, blocked):** the overlay activation forces an
ancestor re-render (preference → `@State` on the shell → whole TabView subtree),
which re-invokes each tab's `.navigationDestination` builder. Sudoku's builder
returns a FRESHLY CONSTRUCTED `AnyView(BoardLoaderView(...))` every call
(`LiveRouteFactory.view(for:path:)` → `boardDestination`), so the pushed board
has no stable identity across ancestor re-renders and is replaced/unmounted.
This is the same per-render-factory shape #909 and #918 already burned this repo
on. iOS is unaffected because its board comes from the `fullScreenCover` built
off `activeGameRoute`, never through the destination builder.

**Next experiment (blocked):** register the overlay but do NOT render the ZStack
sibling — separates "the re-render" from "rendering the hoisted content".

**Independent defect found on the way** (fix regardless of trigger):
`.onDisappear { coordinator?.dismiss() }` in the modifier destroys the hoisted
overlay whenever the board's view is transiently torn down. The overlay lives in
the ANCESTOR precisely so it can outlive the board's own subtree churn; letting a
descendant's disappear kill it is self-defeating and is what turns "board
flickered" into "no overlay at all".

**Harness note (negative result worth keeping):** a bare `NSHostingView`
reproduction (the `ScaledSpacingTests` technique) does NOT reproduce this — it
mounts no real `sidebarAdaptable`/NavigationSplitView machinery, so the board
never genuinely unmounts and the test passes. I deleted it rather than leave a
green test that guards nothing. Ground truth here requires the macOS E2E.

## 偏離 (Deviations)

- **Touched `App/*/Resources/Localizable.xcstrings` + regenerated the SudokuKit
  L10n fixture** — the dispatch's acceptance list wants
  `git diff --stat -- Packages/SudokuKit` empty AND `mise run scan:l10n` green.
  These are in tension: `mise-tasks/scan/l10n` step 3 requires
  `Packages/SudokuKit/Tests/SudokuAppCompositionTests/Resources/Localizable.xcstrings.json`
  to be a **byte-copy** of the shipping Sudoku catalog, so adding `Progress` /
  `Achievements` to that catalog forces a `mise run gen:l10n_fixture` regen (the
  documented task — the fixture is never hand-edited). Chose to keep the CI gate
  green; the SudokuKit diff is exactly one generated fixture file, no source.
  Leader confirmed this deviation is accepted.
  - Keys added: Sudoku gains `Progress` + `Achievements` (it already had
    `Today` / `Practice`); Minesweeper gains `Today` + `Progress` +
    `Achievements`. `Today`'s values are copied verbatim from Sudoku's existing
    entry so the two catalogs agree (glossary consistency, per the
    `ai-translated-localization` skill's Step 4).
  - Spliced as raw text at a single unambiguous anchor rather than round-tripped
    through `json.load/dump` — the skill names that exact footgun, and a
    round-trip here is provably lossy: these catalogs mix `—`-escaped and
    literal em-dashes, so no single serializer reproduces them byte-for-byte.
    Diff is 240 additive lines, zero deletions.
  - Translations needing judgment (flagged for review): `Progress` →
    ja `進捗`, ko `진행 상황`, th `ความคืบหน้า`. `Achievements` uses Apple's own
    Game Center terms (zh `成就`, ja `達成項目`, ko `업적`, es `Logros`).

## 折衷 (Tradeoffs)

- **`present(_:)` takes a `() -> AnyView` builder rather than storing the
  generic content** — considered making `BoardModalOverlayCoordinator` generic
  over the overlay content type; rejected because the coordinator lives in
  `EnvironmentValues` (which cannot carry a per-board generic parameter) and is
  shared by both games' differently-typed overlays. `AnyView` is already the
  established erasure in this shell (`RouteFactory.view(for:path:) -> AnyView`).

- **`BoardModalOverlayCoordinator.HoistedOverlay` is a top-level type** — nesting
  it would still satisfy SwiftLint's `nesting` rule (depth 1), but top-level
  keeps the `hoisted.view.id(hoisted.id)` call site in `RootShellView` readable
  and avoids a depth-2 typealias trap like the one `RootShellViewGenericityTests`
  documents.

## 未決 (Open questions)

- **Practice tab symbol** — see the first design decision: repo `dice` vs
  prototype's 9-cell grid. Defaulted to `dice`. Risk if wrong: a one-line change
  in `AppTab.systemImage`, no API impact.

- **The hoisted overlay's rendered behavior is not unit-tested** — what IS
  covered: the coordinator's present/dismiss transitions, per-presentation
  identity, the environment key's `nil` default, and a compile sentinel for the
  modifier. What is NOT: that a paused board actually leaves the sidebar/tab rows
  un-tappable while its own CTA stays live. That is not reachable from a headless
  SwiftUI test (no lifecycle events fire without a render host), which is exactly
  why #1019 had to drive it interactively. #1020's own acceptance list already
  carries the two interactive checks before merge (idb on iPad, XCUITest
  coordinate-clicks on macOS) — flagging so the Leader does not read the green
  unit suite as covering that guarantee.

- **⚠️ Minesweeper's catalog has NO "Settings" key** (Sudoku has it, 7 locales).
  The new gear renders `Label("Settings", …)` from shared code against
  `Bundle.main`, so on Minesweeper it will render the raw English in all 6
  non-English locales. `mise run scan:l10n` stays GREEN because its shared-code
  gate is scoped to DOTTED keys — this is blind spot #1/#2 named in the
  `ai-translated-localization` skill, hit for real. This is also pre-existing:
  MS's own Settings screen title has the same gap today. I could not fix it —
  `App/Minesweeper` belongs to another agent this session. Values to add (copied
  from Sudoku for glossary consistency): en `Settings` / zh-Hant `設定` /
  zh-Hans `设置` / ja `設定` / ko `설정` / es `Ajustes` / th `การตั้งค่า`.

- **`GameConfig.title` was removed** (see design decisions). If Stage B finds it
  needs a window title on macOS that per-tab `.navigationTitle` cannot supply,
  that is a one-field re-add, not a redesign.

## Stage B — Sudoku (2026-08-25)

Status: COMPLETE
Owner: Developer subagent (Stage B, Sudoku)
Dispatched by: Leader

Scope: `Packages/SudokuKit/**`, `App/Sudoku/**`, `App/SudokuE2ETests/**`,
`App/UITestsShared/**`, `mise-tasks/store/screenshots` (Sudoku lines).
Migrates SudokuKit onto Stage A's shared 3-tab shell.

### 設計決定

- **`makeTabRoot` takes individual deps, not the whole `GameDeps` bag** —
  `SudokuAppComposition.makeTabRoot(tab:puzzleProvider:persistence:
  errorReporter:telemetry:adProvider:adGate:rootViewModel:)` in the new
  `Live+TabRoots.swift`. `GameConfig.makeTabRoot`'s closure signature (fixed by
  Stage A) is `(AppTab, GameDeps, GameRootViewModel<Route>) -> AnyView`; Live.swift's
  closure destructures `deps` before calling this narrower function. Chosen so
  `RouteFactoryTests.makeTabRootYieldsViewForEachTab` can construct it with the
  same lightweight Fakes the rest of that file already uses, instead of having
  to fabricate `GameDeps`'s unrelated fields (audio, reminders, monetization
  controller, toast) that Today/Practice/Progress never touch.
- **`LiveRouteFactory.themedBanner` became a `static func(adProvider:adGate:)`**
  — Today/Practice's banners moved out of `LiveRouteFactory.view(for:)` into
  `Live+TabRoots.swift`, which has no `LiveRouteFactory` instance to call
  through, only the wired deps. Static keeps one banner implementation instead
  of two copies.
- **`StatsView` became generic over a `gameCenterSection: some View`** slot
  (default `EmptyView`) rather than adding an `AchievementsRow?` optional
  parameter — mirrors `DailyHubView`/`PracticeHubView`'s existing `Banner: some
  View` generic-slot pattern in this same module, and keeps `SudokuUI` free of
  a `GameAppKit` import (`AchievementsRow` is supplied from the composition
  layer, not named inside `StatsView`).

### 偏離

- **`DailyHubViewModel.dismissExhausted()` redesigned, not just rewired** —
  design.md §3.1's exhausted row says Cancel "關閉 block，留在 Today" (closes
  the block, stays on Today). Pre-#1020 it popped the shared `path` (Daily was
  a route pushed from Home, so popping landed back on Home). Post-#1020 the
  Daily hub IS the Today tab's root — nothing is ever pushed on top of it while
  `.exhausted` is showing, so the old pop is structurally a no-op now: Cancel
  would do nothing and the exclamation-mark block would stay on screen forever,
  contradicting the spec. Fixed by dropping `state` to `.loaded([])` (an empty,
  contentless hub) instead of touching `path`. This is scope creep beyond the
  dispatch's literal instruction ("Keep... `.board`/`.completion` pushes as
  they are" only named the Practice CTA explicitly), but leaving Cancel dead
  was a regression I would have shipped silently otherwise. Covered by
  `DailyHubViewModel.dismissExhaustedClosesBlockToEmptyLoadedState` /
  `...IsNoOpWhenNotExhausted` (both suites) and the E2E `test_dailyExhausted
  CancelLandsOnHome_N4` (renamed comment, same body — still asserts the
  `sudoku.dailyHub.exhausted` anchor disappears and `game.today.root` stays).
- **TodayTabViewTests baselines recorded under `swift test`, not Tuist/xcodebuild**
  — no dedicated SudokuKit unit-test scheme exists in the generated workspace
  (`xcodebuild -workspace Game.xcworkspace -list` shows only `Sudoku` /
  `Sudoku-E2E` / `SudokuE2ETests`, no bare-package scheme). Per dispatch
  instructions this was recorded via `swift test` and flagged. **Superseded
  mid-task**: #1036 (GameShellKit `AssetCatalogCompiler` build-tool plugin)
  merged into this branch while Stage B was in flight, making `swift test`
  render themed colors correctly (verified: `snapshotIPhoneLight` shows real
  green/red/orange difficulty dots on a warm-cream background, not blank
  white). The FIRST recording pass (pre-merge) produced genuinely blank PNGs;
  those were deleted and re-recorded post-merge. Final baselines are real.
- **4 `StatsViewTests` `.txt` structure baselines re-recorded** — direct,
  mechanical consequence of making `StatsView` generic (`StatsView` →
  `StatsView<EmptyView>` in the NSHostingView mangled-type-name dump
  `assertViewStructure` pins). Not a "someone else's baseline" — these are
  Stats-specific baselines for a type I changed in this same PR. `.png` image
  assertions for the same 4 tests were untouched/still matched (only the
  `.txt` structural dump differed), confirming no visual regression.

### 折衷

- Considered giving `DailyHubViewModel.dismissExhausted()` its own new
  `DailyHubState` case (e.g. `.dismissed`) instead of reusing `.loaded([])`;
  rejected — `.loaded([])` already renders correctly via the existing
  `DailyHubShellView` grid (zero cards, no message), needs no new shell
  branch, and reads naturally as "nothing to show right now," which is what
  the state actually is.

### 未決

- **Leader should re-run the two interactive verifications** (#1019 carryover:
  idb on iPad, XCUITest coordinate-clicks on macOS) for Sudoku's board overlay
  hoisting once Minesweeper's mirror lands — this subagent has no simulator
  access.
- **`dismissExhausted`'s new behavior has no E2E assertion that the block
  visually disappears while staying scrolled to the same position** — the
  existing `test_dailyExhaustedCancelLandsOnHome_N4` only checks the
  `sudoku.dailyHub.exhausted` anchor is gone and `game.today.root` exists;
  Leader may want an additional idb screenshot check that nothing looks broken
  (e.g. a jarring empty-grid flash) when running the interactive pass above.

## Stage B — Minesweeper (2026-08-25)

Status: COMPLETE
Owner: Developer subagent (Stage B, Minesweeper)
Dispatched by: Leader

Scope: `Packages/MinesweeperKit/**`, `App/Minesweeper/**`,
`App/MinesweeperE2ETests/**`, `mise-tasks/store/screenshots` (Minesweeper
lines). Mirrors Stage B — Sudoku's shape onto MinesweeperKit 1:1.

### 設計決定

- **MS never had a `.home` `AppRoute` case** (unlike Sudoku, which had one
  explicit no-op case) — Home was always the universal mode-card surface
  `makeGameApp` built, never a pushed route. So the AppRoute cleanup here is
  simpler than Sudoku's: delete `.daily`/`.practice`/`.stats`, nothing else.
- **`makeTabRoot` takes individual deps, not the whole `GameDeps` bag** —
  mirrors Sudoku's `SudokuAppComposition.makeTabRoot` exactly, for the same
  reason: `LiveRouteFactoryTests.makeTabRootYieldsViewForEachTab` constructs
  it with the same lightweight fakes the rest of that file already uses
  (`FakeGameCenterClient`, `FakePersistence`, `FakePrivateCKGateway`-backed
  stores) instead of fabricating unrelated `GameDeps` fields.
- **`LiveRouteFactory.bannerSlot` became a `static func(adProvider:adGate:)`
  but STAYS optional-typed** (`(any AdProvider)?`, `AdGate?`) — a deliberate
  divergence from Sudoku's non-optional `themedBanner(adProvider:adGate:)`.
  MS's `LiveRouteFactory.init` already makes `adProvider`/`adGate` optional
  (preview/test callsites construct `LiveRouteFactory()` with no args), so
  the `.settings` case's existing call site needs the optional-safe
  `if let adProvider, let adGate` branch preserved; `Live+TabRoots.swift`
  passes the deps bag's non-optional values in, which promote to `Optional`
  automatically at the call site.
- **`MinesweeperStatsView` became generic over `gameCenterSection: some View`**
  — exact mirror of Sudoku's `StatsView<GameCenterSection: View>` change,
  same default-`EmptyView()` pattern.
- **No `selectTab` closure added to `MinesweeperDailyHubViewModel`** — grepped
  `Packages/MinesweeperKit/Sources` for `path.append(.practice)` /
  `path.append(.daily)` and read `MinesweeperDailyOpenGuardView.swift` in
  full: MS has no `.exhausted`/generator-failure state (D21 — MS trio
  generation is synchronous/non-throwing) and no hub code ever pushed
  `.practice`/`.daily`/`.stats` as a tab swap. The guard view's `path` param
  is used only for `exitToHub()` (a pop), untouched by this migration. This
  is the one item the Stage A dispatch flagged as conditional ("if some MS
  hub code pushed...") — confirmed not applicable.

### 偏離

- **`GameConfig.settingsRoute` landed mid-task** (a concurrent Stage-A-shared
  change, `TabRootChrome` / shared Settings gear, #1020 design.md §2.1/§3.7)
  — `swift build` first failed on a missing `settingsRoute` argument that
  wasn't part of the original dispatch brief. Added
  `settingsRoute: .settings,` to `Live.swift`'s `GameConfig` call, mirroring
  the same line the Sudoku developer had already added. No other adaptation
  needed — `TabRootChrome` attaches generically via `chromedTabRoots` inside
  `makeGameAppCore`, MS's tab roots needed no changes for it.
- **`AppRouteTests.homeRoutesAreDistinct` / `sameCaseRoutesAreEqual` deleted
  outright** rather than trimmed — with only `.settings` left as a
  payload-free case, "distinct from each other" has nothing left to compare
  against; replaced with a single trivial `settingsEqualsItself` test (its
  distinctness from a payload-bearing route was already covered by the
  pre-existing `settingsIsDistinctFromBoard`).

### 折衷

- Considered keeping `LiveRouteFactory+Stats.swift` as a file and just gutting
  its content vs. deleting it outright and folding the `.progress` case into
  `Live+TabRoots.swift`. Deleted — the file existed purely to keep
  `LiveRouteFactory.swift` under the 400-line SwiftLint ceiling for a case
  that no longer lives in that switch; keeping an empty/near-empty extension
  file around would be dead weight.

### 未決

- None load-bearing. The two interactive-verification items Stage A flagged
  (idb iPad, XCUITest macOS coordinate-clicks) apply identically to
  Minesweeper's board overlay hoisting (`MinesweeperBoardView.boardModalOverlay`)
  — same `#1019`/`#1020` seam, same "not unit-testable" caveat; Leader's
  planned interactive pass should cover both apps in one session since they
  share the exact same `RootShellView`/`BoardModalOverlayCoordinator` code.

## CR fix round (2026-08-26)

Status: COMPLETE
Owner: Developer subagent (Stage B, Sudoku — also touched MinesweeperKit +
GameAppKit for this round per the dispatch)

### 偏離

- **`TodayTabHostTests` cannot drive `onAdContext` by rendering** — tried the
  `NavigationPreferencePropagationTests` pattern (offscreen `NSWindow` +
  `RunLoop.main.run(until:)` pump). Four diagnostics isolated a genuine
  harness limit, not a code bug: a plain synchronous `.task` fires reliably
  in that harness (0.1s), but a `.task` whose body `await`s across an actor
  boundary (`AdGate.shouldShowBanner`, `FakeAdProvider` — both actors) never
  resumes, confirmed hung past a 10s pump. Looks like `swift-testing`'s async
  test executor and `RunLoop.main` aren't the same executor in a headless
  `swift test` process (no `NSApplication` ever bootstraps). Took the
  dispatch's own sanctioned fallback: factored `TodayTabHost.bannerSlot`'s
  one-line `onAdContext` body into a free function,
  `todayTabHostFireOnAdContext(attPrimer:)` (zero behavior change —
  `bannerSlot` now calls it instead of duplicating it), so `TodayTabHostTests`
  calls the EXACT function production uses, without rendering. NOT covered:
  whether `BannerSlotView` itself withholds calling `onAdContext` while its
  gate is closed — that guard lives in a pre-existing, unmodified shared
  component (`BannerSlotView.resolveGateAndLoad`'s `guard allowed else {
  return }`), verified by reading the source, not a live test (exercising it
  behaviorally hits the same harness limitation). Flagging for Leader:
  closing this specific gap would mean adding a similar testable seam to
  `BannerSlotView` itself in MonetizationUI, which felt like scope creep
  beyond "C-33 needs its own test" for a pre-existing, unmodified component.
- **Free function, not a `static func` on `TodayTabHost`** — a first attempt
  made it `static func fireOnAdContext(attPrimer:)` on the generic
  `TodayTabHost<Route, Content>` struct; `bannerSlot`'s closure calling
  `Self.fireOnAdContext(...)` still tripped the same `Content.Type`
  Sendable-metatype warning capturing `self`/`Self` would. A free top-level
  function (taking `attPrimer` as a plain parameter) has no generic metatype
  to capture and compiles warning-free.

### 折衷

- Considered leaving the double-banner bug fix scoped to just deleting the
  `banner:` argument (no fixture change) — rejected because every existing
  `TodayTabViewTests`/`MinesweeperTodayTabViewTests` baseline seeds the ad
  gate CLOSED, so the bug was invisible in every committed snapshot (both
  banners collapse to the same `EmptyView`). Without a gate-open fixture, a
  regression back to double-rendering would ship unnoticed again.
