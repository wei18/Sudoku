# Impl Notes — #572 MS migration to GameConfig/makeGameApp (2026-06-19)

Status: COMPLETE
Owner: Developer (Sonnet 4.6)
Dispatched by: Leader
Started: 2026-06-19T00:00:00Z

## 設計決定 (Design decisions)

- **MinesweeperAppComposition field shape** — Tests access `.routeFactory`, `.telemetry`, `.errorReporter`, `.persistence`, `.adProvider`, `.iapClient`, `.adGate`, `.monetizationStateStore`, `.monetizationController`, `.toastController`. All must stay as public fields (fed from `wired.deps`). Adding `wiredView: AnyView` private + `rootView: some View` public accessor mirroring Sudoku. `rootViewModel` type stays `MinesweeperRootViewModel` (already a typealias `= GameRootViewModel<AppRoute>`).

- **`MinesweeperReminderSettingsEntry` deletion** — Defined inside `MinesweeperUI/SettingsView.swift`. Removing it + replacing usages with `ReminderSettingsEntry` (from SettingsUI) throughout `SettingsView.swift`, `LiveRouteFactory.swift`, and `MinesweeperSettingsViewTests.swift`.

- **`makeReminderSettings` closure type change** — `LiveRouteFactory.makeReminderSettings` field changes from `(@MainActor () -> MinesweeperReminderSettingsEntry)?` to `(@MainActor () -> ReminderSettingsEntry)?`. After migration, Live.swift passes `deps.makeReminderSettings` (which returns `ReminderSettingsEntry` from SettingsUI).

- **`puzzleLoader` sentinel** — `MinesweeperLivePuzzleLoaderUnavailable` private struct stays in the new `Live.swift`.

- **Home snapshot tests** — Checked Sudoku's post-#557 home snapshots to understand `GameHomeView` construction. Need to migrate `MinesweeperHomeSnapshotTests` + `ASCScreenshotEmitTests`.

## 偏離 (Deviations)

- **Keep `MinesweeperRootViewModel.swift`** — Spec lists for deletion; keeping it because `MinesweeperRootViewModelTests.swift` imports `@testable import MinesweeperUI` and uses the `MinesweeperRootViewModel` name. It's a 1-line typealias, zero behavior. Sudoku's equivalent `RootViewModel.swift` was also kept.

- **`LiveRouteFactory.makeReminderSettings` type change** — Required by `MinesweeperReminderSettingsEntry` deletion. Behavioral parity preserved.

- **Home snapshot tests** — `MinesweeperHomeSnapshotTests` + `ASCScreenshotEmitTests` migrate from `MinesweeperHomeView(viewModel: MinesweeperHomeViewModel())` to `GameHomeView`. The spec allows `.txt` type-name deltas for snapshot baselines.

## 折衷 (Tradeoffs)

- **`MinesweeperRootViewModel` kept as typealias** — Deleting would break test compilation with no benefit; keeping costs 1 line. Matches Sudoku's RootViewModel.swift precedent.

## 未決 (Open questions)

All resolved. No open questions.

## 補充 (Post-commit notes)

- **`HomeModeContent<AppRoute>` explicit type required** — Swift cannot infer `Route` from the dict key when a `.leaderboard` entry has no `route:` arg. Fixed by spelling out `HomeModeContent<AppRoute>(...)` on all entries in 3 test files.
- **`HomeMode` + `ATTPrimerCoordinator` missing imports** — `MinesweeperHomeSnapshotTests` needed `import GameShellUI` (HomeMode) + `import MonetizationUI` (ATTPrimerCoordinator). `MinesweeperHomeViewModelTests` referenced deleted `MinesweeperHomeMode.allCases`; replaced with `HomeMode.allCases`.
- **`.txt` baselines re-recorded** — Old `MinesweeperHomeView` type-name references replaced with `GameHomeView`; deleted stale baselines, re-ran tests with `.missing` record mode. 0 PNGs changed.
- **Commit sha**: `9d56e07` — 20 files changed, 560 insertions, 1078 deletions. All 3 pre-commit hooks passed (gitleaks, hygiene, swiftlint strict).
