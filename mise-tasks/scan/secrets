#!/usr/bin/env bash
#MISE description="gitleaks pre-commit / pre-build secret scan over staged files."
set -euo pipefail
exec mise exec aqua:gitleaks/gitleaks -- gitleaks git --pre-commit --staged --redact --verbose
